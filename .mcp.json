{
  "mcpServers": {
    "devdigest": {
      "command": "sh",
      "args": ["-c", "cd mcp && exec npx tsx src/server.ts"],
      "env": {
        "DEVDIGEST_API_URL": "http://localhost:3001"
      }
    }
  }
}
